services:
  nginx-proxy:
    image: jwilder/nginx-proxy
    container_name: nginx-proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ./data/certs:/etc/nginx/certs:ro
      - ./data/vhost.d:/etc/nginx/vhost.d
      - ./data/html:/usr/share/nginx/html
    labels:
      - com.github.nginx-proxy.nginx
    networks:
      - proxy

  acme-companion:
    image: nginxproxy/acme-companion
    container_name: acme-companion
    restart: always
    environment:
      - DEFAULT_EMAIL=jeonghyun.bae@kakao.com
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./data/certs:/etc/nginx/certs
      - ./data/vhost.d:/etc/nginx/vhost.d
      - ./data/html:/usr/share/nginx/html
    depends_on:
      - nginx-proxy
    networks:
      - proxy

  frontend:
    build: ./frontend
    container_name: localbasket-frontend
    expose:
      - "80"                          # nginx-proxy가 내부로 붙음
    environment:
      - VIRTUAL_HOST=localbasket.o-r.kr
      - LETSENCRYPT_HOST=localbasket.o-r.kr
      - LETSENCRYPT_EMAIL=jeonghyun.bae@kakao.com
      - VIRTUAL_PORT=80
    depends_on:
      - backend
    networks:
      - proxy

  backend:
    build: ./backend
    container_name: localbasket-backend
    expose:
      - "9000"                        # 외부 publish 불필요, 프록시만 접근
    volumes:
      - ./backend/uploads:/app/uploads
    environment:
      # ⚠️ VIRTUAL_HOST/VIRTUAL_PATH 제거 (프론트 한 곳만 호스트를 소유)
      - SERVER_PORT=${SERVER_PORT:-9000}
      - DB_URL=${DB_URL}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-https://localbasket.o-r.kr,http://localhost:8080}
      - CSP=${CSP}
    networks:
      - proxy
    # 호스트 공개가 필요 없다면 포트 매핑 생략
    # ports:
    #   - "${SERVER_PORT:-9000}:9000"

networks:
  proxy:
    driver: bridge
